faster-whisper
sounddevice
numpy
deep-translator
argostranslate
fastapi
uvicorn[standard]
soundfile
requests
# mlx-whisper ставится отдельно в install.sh: на macOS < 13.5 его нет,
# и это не должно валить установку (движок умеет работать без него)
